# Slope-stability-by-Fellenius-Method
Python-based slope stability analysis tool developed in Anaconda. The program accepts user-defined slope geometry parameters, automatically generates the slope and slip surface visualization, and calculates the Factor of Safety (FOS).
